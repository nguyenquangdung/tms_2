<% provide(:title, "All Subjects") %>
<h1>Subjects</h1>

<h2><%= link_to "Create new subject", new_supervisors_subject_path %></h2>

<ul class="subjects">
  <% @subjects.each do |subject| %>
    <li>
      <div><%= link_to subject.subject_name, supervisors_subject_path(subject) %></div>
      <div><%= subject.description %></div>
      <div>
        <%= link_to "edit", edit_supervisors_subject_path(subject) %>
        <%= render 'shared/delete_form', object: supervisors_subject_path(subject)  %>
      </div>
    </li>
  <% end %>
</ul>